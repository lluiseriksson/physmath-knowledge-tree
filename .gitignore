.DS_Store
Thumbs.db
node_modules/
dist/
coverage/
*.log
.env
.env.*
!.env.example
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
# Local route-signing private keys; publish only deliberately copied public keys.
.route-keys/
# Local source-curation inbox; records store hashes, not raw files.
curation/inbox/*
!curation/inbox/.gitkeep
# Local reproducible-run manifests and logs; export or copy deliberately.
.run-ledger/
