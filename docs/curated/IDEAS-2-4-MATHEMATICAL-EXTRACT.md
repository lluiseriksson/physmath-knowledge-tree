# Curated mathematical extract — `ideas 2(4).txt`

## Source disposition

The source is a Python wrapper containing a 1,287-line research context for another fast-moving repository. The mathematical core is compact, but it is mixed with historical repository status, proposed commit campaigns, source-shopping notes and speculative branches. The raw wrapper is therefore **not included** in the canonical repository.

Canonical provenance: [`curation/records/ideas-2-4.json`](../../curation/records/ideas-2-4.json).

## Exact target-erasure diagnostic

The central negative result is the factorization

\[
V(r,n)=
\left(\sum_{P:\,r\in\operatorname{skeleton}(P)}w(P)\right)
\left(\sum_Pw(P)\right)^n.
\]

After this projection, the exact target \(Y\), tree edges and parent-child incompatibility have disappeared. A uniform factor depending on \(d_M(Y)\) cannot be derived from this object alone without reintroducing target information as an assumption. This is represented by `bridge.target_erasure_diagnostic`.

## Target-preserving proof discipline

For a second Ursell term, retain the exact union fiber, a spanning incompatibility tree, the marked root and local parent-child relations until metric stitching extracts the target exponential. Global overcounting is safe only after the target contribution has been consumed. This is represented by `bridge.target_preserving_ursell_summation` and `move.preserve_exact_target`.

## Rooted leaf recursion

Let \(j_v\) be the number of children of a rooted tree vertex. Under explicit rooted and parent-child moment bounds,

\[
\sum_{X:\,r\in\operatorname{skeleton}(X)}m(X)^j e^{-2\kappa_0m(X)}
\le j!C_2^{j+1},
\]

and

\[
\sum_{X\not\sim P}m(X)^j e^{-2\kappa_0m(X)}
\le j!C_2^{j+1}m(P),
\]

bottom-up elimination has the conditional fixed-tree form

\[
\text{assignment sum}\le C_2^{2n+1}\prod_vj_v!.
\]

The tree-shape sum is a separate theorem. This separation prevents a global leaf mass from hiding volume dependence. It is represented by `bridge.rooted_leaf_factorial_moments`.

## Explicit rate budget

The algebraic bookkeeping is

\[
\kappa-\kappa_0-2
=(\kappa-3\kappa_0-3)+(2\kappa_0+1).
\]

The first term is residual target decay; the second is the reserve for local moments and explicit margins. If the normalized tree count contributes \(4^n\) and the first activity supplies \(\varepsilon^{n+1}\), the fixed-order profile is conditionally

\[
C_2\varepsilon e^{-\rho m(Y)}(4C_2^2\varepsilon)^n.
\]

For \(4C_2^2\varepsilon<1\), the geometric amplitude is

\[
\frac{C_2\varepsilon}{1-4C_2^2\varepsilon}e^{-\rho m(Y)}.
\]

The algebra is retained; exact source constants and semantic identification of the first activity remain in the verification queue.

## Producer versus consumer separation

The target-sensitive tree sum is model-independent. It does not construct the first gauge activity. The concrete Yang-Mills producer separately requires the one-step fluctuation identity, gauge precision, coercivity, covariance decay, collar localization, derivative and regulator bounds, and the local R-operation gain. Existing Ward, Gaussian, block-coercivity and physical-scaling nodes were enriched but not duplicated.

## Deletion status

The source is marked `delete-after-user-review`. Its wrapper hash, promoted claims, quarantine items and discard reasons are recorded. After review, the original may be removed without storing the full context in Git.
