# Prompt: extract the missing Balaban–Dimock source theorem

Use the Yang–Mills source map and hypothesis frontier. Identify, for every
source anchor, the exact mathematical object, Lean field, downstream consumer,
constant dependencies and quantifier order.

Required outputs:

- Gaussian pushforward as an equality of measures;
- concrete covariance/root certificate with kernel localization;
- typed Wilson-Hessian identification;
- actual local activity construction;
- raw pointwise modified-metric decay with cutoff domain;
- separated support localization;
- strong measurability and integrability;
- Appendix-F with-holes object matching;
- rooted H# physical remainder identity;
- one constants package uniform in volume, cutoff, scale, background, holes and root position;
- a final Lean structure and projection theorem shape.

For every item mark one of: `Lean-verified`, `source theorem-shaped`,
`source-backed candidate`, `translation missing`, or `open identification`.
Never use a citation as a proof term and never replace the list by one opaque
`Prop`.
